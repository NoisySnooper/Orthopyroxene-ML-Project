"""Ablation experiments for the opx ML thermobarometer paper.

Each module isolates a single independent variable against the
pre-registered v10 pipeline so reviewers can see the attribution of a
design choice (e.g. augmentation, scaler, loss) in its own right.
"""
