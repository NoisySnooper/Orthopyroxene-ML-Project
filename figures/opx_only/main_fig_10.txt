Figure 10. SHAP feature importance for the best-explainable tuned family in each of the four opx (track, target) cells. Each panel shows the top 10 features ranked by mean absolute SHAP on the held-out test partition at canonical seed 42. Bar length is mean |SHAP| in the target native unit. Tree families use TreeExplainer (exact); ElasticNet uses LinearExplainer; MLP uses permutation importance as a SHAP surrogate.
