Fig. 30. Per-regime test-set RMSE before and after bias correction for the 8 the final phase cells. Grid rows: T_C (top), P_kbar (bottom). Columns: opx-liq, opx-only, cpx-liq, cpx-only. Each regime group shows pre-correction (gray), post Form A (blue), post Form B (vermillion). Bar heights are 20-seed means; vertical error bars are the mean of per-seed bootstrap 95% CIs. Hatched bars mark regimes with n < 20 (sample-size limited). "accepts Form A or Form B" annotation marks the final phase shipping decision at canonical seed 42. Source CSVs: results/bias_correction_{per_seed,summary,shipped}.csv.
